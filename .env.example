# OpenAI-compatible model service. Do not commit a populated .env file.
BASE_URL=https://api.example.com/v1
API_KEY=replace-with-your-api-key
BASE_MODEL=your-model-name

# Optional agent runtime limits.
TOOL_TIMEOUT_MS=30000

# Optional local QQ Chat Exporter defaults.
QCE_TOKEN=
QCE_PATH=
